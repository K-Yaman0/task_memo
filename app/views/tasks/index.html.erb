<h1>トップページ</h1>
<%= link_to '投稿する', new_task_path %>
<ul>
  <% @tasks.each do |task| %>
    <li>
    <%= link_to task_path(task.id) do %>
      <%= task.content %>
    <% end %>  
    </li>
  <% end %>
</ul>
